# Runtime dependencies for hosted Streamlit/Railway builds.
#
# Railway installs requirements before copying the full source tree into the
# build image, so an editable install (-e .) cannot see src/ at that stage.
sqlmodel>=0.0.16
streamlit>=1.30
Authlib>=1.3.2
pandas>=2.0
plotly>=5.18
numpy>=1.26
httpx>=0.27
python-dotenv>=1.0
apscheduler>=3.10
typer>=0.12
psycopg[binary]>=3.1
